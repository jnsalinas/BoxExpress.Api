namespace BoxExpress.Domain.Entities;

// No se va a usar, era para los retiros, se deja porque despues se usara
public class Bank : BaseEntity
{
    public string Name { get; set; } = string.Empty;
}